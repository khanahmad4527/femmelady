import { Stack, Title } from '@mantine/core';
import commonClasses from '~/styles/Common.module.scss';

const Checkout = () => {
  return <Stack className={commonClasses.consistentSpacing}>
    <Title></Title>
  </Stack>;
};

export default Checkout;
